import type { DatePickerConfig } from '../types/date-picker.type.js';
export declare const DEFAULT_CONFIG: DatePickerConfig;
export declare const TIME_FORMATS: {
    hour24: string;
    hour12: string;
};
export declare const MONTH_FORMATS: {
    short: string;
    long: string;
};
